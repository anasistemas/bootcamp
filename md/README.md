# Markdown Preview Tool

Convierte archivos markdown a HTML.

## Uso

go run main.go -in archivo.md -out nombre_salida

## Flags

- `-in` (obligatorio): ruta al archivo markdown de entrada
- `-out` (opcional): nombre del archivo HTML de salida sin extensión. Si no se pasa, se usa el nombre del archivo de entrada